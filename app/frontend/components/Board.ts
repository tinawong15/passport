export class Board { 
    static boardTest() {
        let text = document.createTextNode("TEST");
        document.getElementById("board-text").appendChild(text);
        console.log("TEST");
    }
}